server port 3000

database connect "mongodb://localhost/chat-demo"

middleware cors
middleware logs

route GET /health
  return ok

socket /chat
  on message
    broadcast message

socket /notifications
  on update
    broadcast update
